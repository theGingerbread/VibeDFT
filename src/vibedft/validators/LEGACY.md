# Legacy Module

`src/vibedft/validators/` is retained as pre-v2 migration inventory.

Do not extend this directory for new platform work until validation rules are
re-expressed against the v2 task and cleaned-result contracts.
