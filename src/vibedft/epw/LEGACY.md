# Legacy Module

`src/vibedft/epw/` is retained as pre-v2 migration inventory.

Do not extend this directory for new platform work. EPW-related logic should be
reintroduced through `calculator/` and `analysis/` boundaries.
