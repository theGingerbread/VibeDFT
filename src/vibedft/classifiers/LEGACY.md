# Legacy Module

`src/vibedft/classifiers/` is retained as pre-v2 migration inventory.

Do not extend this directory for new platform work. Re-express migrated
capabilities inside `src/vibedft/calculator/` or `src/vibedft/_shared/`.
