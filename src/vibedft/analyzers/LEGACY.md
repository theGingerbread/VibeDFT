# Legacy Module

`src/vibedft/analyzers/` is retained as pre-v2 migration inventory.

Do not extend this directory for new platform work. Re-express migrated
capabilities inside `src/vibedft/analysis/`.
