# Legacy Module

`src/vibedft/generators/` is retained as pre-v2 migration inventory.

Do not extend this directory for new platform work. New generation logic should
land in task-scoped calculator packages.
