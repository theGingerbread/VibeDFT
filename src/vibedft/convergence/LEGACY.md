# Legacy Module

`src/vibedft/convergence/` is retained as pre-v2 migration inventory.

Do not extend this directory for new platform work until the capability is
re-expressed against cleaned-result contracts.
