# Legacy Module

`src/vibedft/agent/` is retained as pre-v2 migration inventory.

Do not extend this directory for new platform work. Re-express migrated
capabilities inside the v2 canonical surface.
