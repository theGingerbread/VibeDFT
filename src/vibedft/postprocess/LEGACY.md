# Legacy Module

`src/vibedft/postprocess/` is retained as pre-v2 migration inventory.

Do not extend this directory for new platform work until postprocessing is
re-expressed against cleaned-result contracts.
